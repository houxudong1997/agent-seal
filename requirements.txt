# agent-seal — tamper-evident audit trail for AI agents
# Core dependencies.
#
# Install:  pip install -r requirements.txt
# For dev:  pip install -r requirements-dev.txt

# ── Web framework (FastAPI + ASGI server) ────────────────────
fastapi>=0.100
uvicorn[standard]>=0.20
sse-starlette>=1.0
starlette>=1.3.1

# ── Observability / Monitoring ───────────────────────────────
prometheus-client>=0.20
prometheus-fastapi-instrumentator>=7.0

# ── Configuration ────────────────────────────────────────────
python-dotenv>=1.0

# ── Cryptography (Ed25519 signing + AES-256-GCM encryption) ──
cryptography>=43.0

# ── Database ORM + Migrations ────────────────────────────────
sqlalchemy>=2.0
alembic>=1.13

# ── Policy engine (YAML-based rules) ─────────────────────────
pyyaml>=6.0

# ── Data validation (API schemas, config models) ─────────────
pydantic>=2.0
