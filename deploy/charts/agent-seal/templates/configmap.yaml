apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-seal.fullname" . }}-config
  labels:
    {{- include "agent-seal.labels" . | nindent 4 }}
data:
  # ── Core settings (non-sensitive) ──
  AGENT_SEAL_STORAGE_BACKEND: {{ .Values.config.storageBackend | quote }}
  AGENT_SEAL_AUDIT_DIR: {{ .Values.config.auditDir | quote }}
  AGENT_SEAL_API_HOST: {{ .Values.config.apiHost | quote }}
  AGENT_SEAL_API_PORT: {{ .Values.api.port | quote }}
  AGENT_SEAL_CORS_ORIGINS: {{ .Values.config.corsOrigins | quote }}
  # ── LLM Tracing ──
  AGENT_SEAL_AUTO_TRACE: {{ .Values.config.autoTrace | toString | quote }}
  AGENT_SEAL_TRACE_PII_REDACT: {{ .Values.config.tracePiiRedact | toString | quote }}
  AGENT_SEAL_TRACE_MAX_LEN: {{ .Values.config.traceMaxLen | quote }}
  AGENT_SEAL_TRACE_COST_MODEL: {{ .Values.config.traceCostModel | quote }}
  # ── Logging ──
  AGENT_SEAL_LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  AGENT_SEAL_LOG_FORMAT: {{ .Values.config.logFormat | quote }}
  # ── Notifications ──
  AGENT_SEAL_NOTIFY_ON_FAILURE: {{ .Values.config.notifyOnFailure | toString | quote }}
  # ── Evidence ──
  AGENT_SEAL_EVIDENCE_STORE: {{ .Values.config.evidenceStore | quote }}
  # ── SMTP host (non-sensitive, port/auth in Secret if needed) ──
  AGENT_SEAL_SMTP_HOST: {{ .Values.config.smtpHost | quote }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-seal.fullname" . }}-nginx-conf
  labels:
    {{- include "agent-seal.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- tpl .Values.nginx.conf . | nindent 4 }}
{{- if .Values.postgresql.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-seal.fullname" . }}-postgresql-init
  labels:
    {{- include "agent-seal.labels" . | nindent 4 }}
data:
  init.sql: |
    {{- .Values.postgresql.initSql | nindent 4 }}
{{- end }}
