apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-seal.fullname" . }}-secret
  labels:
    {{- include "agent-seal.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- /* DB URL: use external or auto-generate from internal PostgreSQL */}}
  AGENT_SEAL_DB_URL: {{ include "agent-seal.dbUrl" . | quote }}
  {{- /* Redis URI */}}
  AGENT_SEAL_REDIS_URI: {{ include "agent-seal.redisUri" . | quote }}
  {{- /* Secret key (CRITICAL — must be set in production) */}}
  AGENT_SEAL_SECRET_KEY: {{ .Values.config.secretKey | quote }}
  {{- /* API keys */}}
  AGENT_SEAL_API_KEYS: {{ include "agent-seal.apiKeysString" . | quote }}
  {{- /* Slack webhook (sensitive URL) */}}
  AGENT_SEAL_SLACK_WEBHOOK: {{ .Values.config.slackWebhook | quote }}
  {{- /* Signing & Encryption keys */}}
  AGENT_SEAL_SIGNING_KEY: {{ .Values.config.signingKey | quote }}
  AGENT_SEAL_ENCRYPTION_KEY: {{ .Values.config.encryptionKey | quote }}
  {{- /* Internal PostgreSQL password (auto-generated when postgresql.enabled=true) */}}
  {{- if .Values.postgresql.enabled }}
  POSTGRES_PASSWORD: {{ .Values.postgresql.auth.password | quote }}
  {{- end }}
